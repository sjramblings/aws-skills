{
  "$schema": "https://json.schemastore.org/claude-code-settings",
  "_comment": "Scaffolded by harness-init. M5 wires the red-test pre-commit block; M6 will add postmortem-capture, session-start brief, and stop learning flush. Do not hand-edit — run /harness-init --upgrade.",
  "enabledPlugins": [
    "aws-harness",
    "aws-cdk",
    "aws-common",
    "aws-cost-ops",
    "serverless-eda"
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit:*)",
        "command": "bash .claude/hooks/pre-commit-red-test-block.sh"
      }
    ]
  }
}
